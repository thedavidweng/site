{
  "name": "site",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "dev": "vitepress dev",
    "build:agent-files": "node scripts/build-money-agent-files.mjs",
    "build": "pnpm build:agent-files && vitepress build",
    "preview": "vitepress preview",
    "sync-docs": "node scripts/sync-docs.mjs",
    "lint": "oxlint .vitepress scripts",
    "lint:fix": "oxlint .vitepress scripts --fix"
  },
  "devDependencies": {
    "oxlint": "^1.69.0",
    "vitepress": "^1.6.4"
  }
}
